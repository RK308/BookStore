<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_add_book"
        android:title="@string/menu_main_add_book"
        android:icon="@drawable/ic_add"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_delete_all_books"
        android:title="@string/menu_main_delete_all_books"
        android:icon="@drawable/ic_delete"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_exit_books"
        android:title="@string/menu_main_exit"
        android:icon="@drawable/ic_exit"
        app:showAsAction="always"/>
</menu>